import withParent from '@/hoc/withParent';

const Cv = () => (
  <div>
    <iframe
      style={{ width: '100%', height: '100rem' }}
      src='/NANDA GOPAL.pdf'
    ></iframe>
  </div>
);

export default withParent(Cv);
